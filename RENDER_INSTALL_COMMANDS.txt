# Render Installation Commands

## Build Command
pip install -r requirements.txt && pip install catboost==1.2.5 --no-deps

## Start Command
cd backend && python run_production_services.py

## Environment Variables
PYTHON_VERSION=3.10.0
